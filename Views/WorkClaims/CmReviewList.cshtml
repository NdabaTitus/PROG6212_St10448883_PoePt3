@model IEnumerable<Poe.Models.WorkClaim>

@{
    ViewData["Title"] = "CM Review Claims";
}

<h2>Claims for Construction Manager Review</h2>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Date</th>
            <th>Worker</th>
            <th>Department</th>
            <th>Total (R)</th>
            <th>Status</th>
            <th>Reject Reason (PM)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var claim in Model)
        {
            <tr>
                <td>@claim.Id</td>
                <td>@claim.CreatedAt.ToString("yyyy-MM-dd")</td>
                <td>@claim.Name @claim.Surname</td>
                <td>@claim.Department</td>
                <td>@claim.TotalAmount.ToString("F2")</td>
                <td>@claim.Status</td>
                <td>@claim.RejectReason</td>
                <td>
                    <a asp-action="CmApprove" asp-route-id="@claim.Id" class="btn btn-success btn-sm">Approve</a>
                    <a asp-action="CmReject" asp-route-id="@claim.Id" class="btn btn-danger btn-sm">Reject</a>
                </td>
            </tr>
        }
    </tbody>
</table>
